# 公开源码前的敏感信息检查

检查日期：2026-09-20。结论：在本次检查范围内，未发现公司内部域名、内部服务地址、业务数据、员工标识或公司凭据。此结论针对当前文件快照，不是对未来导入数据或新增代码的保证。

## 检查范围与结果

- 检查源码、配置、脚本、CI、README、技术文档、依赖锁文件，以及测试 PDF / DOCX / Markdown：54 个源码交付文件（不含本报告）。
- 检查公司相关词、常见 API key / GitHub token / 云访问密钥 / JWT / 私钥格式、带凭据 URL，以及个人绝对路径：未发现敏感候选项。源码中不存在本机用户名或旧工作区绝对路径。
- npm lockfile 的 597 条 resolved 地址均指向公开 npm registry；Cargo lockfile 使用公开 crates.io index。未发现企业私有包源或带凭据的下载地址。
- 截图为本项目示例库或生成的测试场景；PDF 和 DOCX fixtures 是脚本生成的示例。PNG 未发现文本或 EXIF 元数据块。
- 对本地 SQLite 的知识库名、文件路径、批注文本和 68 个 blob（含已替换、删除后尚未回收的测试 blob）进行了本机检查，未发现公司相关内容。
- 保留 EA.KB.IO 的公开来源、参考提交和 MIT 版权声明。这些是合法来源与许可说明，不应作为敏感信息删除。

## 本地材料与公开源码的边界

本地 `data/` 含示例知识库以及会话 token 摘要。构建目录可能含编译时个人路径，浏览器测试产物可能含会话和调试信息。这些不是本次发现的公司信息，但不属于公开源码交付物。

现有 `.gitignore` / `.dockerignore` 已排除数据、依赖、构建产物、测试 trace 和日志；此次额外加入 `.env.*`、数据库及 WAL、私钥/证书容器、备份目录的排除规则，并保留 `.env.example`。已用独立临时 Git 元数据验证 11 类路径的忽略行为，没有给项目初始化或推送 Git 仓库。

测试和 CI 中保留有明确的示例密码，例如 `test-password-123` 与 `folio-local-dev`；它们不是公司凭据，也不是公网部署密码。服务端正式密码仍要求由环境变量提供。

## 迁移验证

项目已迁移至用户指定的 OpenSource 目录，未带入原父工作区的其他项目或 Git 历史。

迁移时先停止本地预览，再原位移动目录；127 个项目/运行文件大小检查及 68 个 blob 的 SHA-256 校验一致。SQLite integrity_check 返回 `ok`，1 个知识库、4 份当前文档保持完整。后台预览已从新路径启动，首页与健康检查均返回 HTTP 200。

公开源码应通过 Git 的受控文件集合交付，不应直接把包含 data、构建缓存和依赖的整个本地目录压缩上传。本次未执行对外上传、发布或部署。
